services:
  - type: web
    name: sechenov-bot
    runtime: python
    buildCommand: pip install fastapi uvicorn requests python-dotenv pydantic
    startCommand: uvicorn bot_server:app --host 0.0.0.0 --port $PORT
